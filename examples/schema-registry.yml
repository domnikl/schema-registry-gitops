compatibility: FULL_TRANSITIVE

subjects:
  - name: User-value
    file: User.avsc
    compatibility: BACKWARD

  - name: with-references
    file: with-references.avsc
    type: AVRO
    references:
      - name: dev.domnikl.schema_registry_gitop.User
        subject: User-value
        version: 1
